import {sqliteTable,text,integer,index} from "drizzle-orm/sqlite-core";
export const leads=sqliteTable("leads",{
id:text("id").primaryKey(), requestKey:text("request_key").notNull().unique(), name:text("name").notNull(), email:text("email").notNull(), phone:text("phone").notNull().default(""),city:text("city").notNull(),postcode:text("postcode").notNull(),segment:text("segment").notNull(),payload:text("payload").notNull(),source:text("source").notNull(),campaign:text("campaign").notNull().default(""),stage:text("stage").notNull().default("nouveau"),assignee:text("assignee").notNull().default("Kevin"),nextAction:text("next_action").notNull().default("Qualifier la demande"),dueAt:text("due_at").notNull(),notes:text("notes").notNull().default(""),checks:text("checks").notNull().default("{}"),marketing:integer("marketing").notNull().default(0),stopped:integer("stopped").notNull().default(0),version:integer("version").notNull().default(0),createdAt:text("created_at").notNull(),updatedAt:text("updated_at").notNull()
},t=>[index("leads_email_created").on(t.email,t.createdAt),index("leads_due").on(t.dueAt)]);
export const history=sqliteTable("history",{id:text("id").primaryKey(),leadId:text("lead_id").notNull().references(()=>leads.id,{onDelete:"cascade"}),at:text("at").notNull(),actor:text("actor").notNull(),description:text("description").notNull()},t=>[index("history_lead").on(t.leadId)]);

