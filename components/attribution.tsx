"use client";import {useEffect} from "react";export function Attribution(){useEffect(()=>{const q=new URLSearchParams(location.search);const s=q.get("utm_source"),c=q.get("utm_campaign");if(s||c){document.querySelectorAll<HTMLAnchorElement>('a[href^="/etude"],a[href^="/proprietaires/"]').forEach(a=>{const u=new URL(a.href);if(s)u.searchParams.set("utm_source",s.slice(0,100));if(c)u.searchParams.set("utm_campaign",c.slice(0,100));a.href=u.pathname+u.search})}},[]);return null}