import {requireChatGPTUser} from "@/app/chatgpt-auth";import {isAdmin} from "@/lib/server";import CRM from "./crm";export const dynamic="force-dynamic";export default async function Page(){await requireChatGPTUser("/pilotage");if(!await isAdmin())return <main className="article-page"><h1>Accès réservé.</h1><p>Cet espace est réservé au compte administrateur Solaya.</p><a href="/">Retour au site</a></main>;return <CRM/>}