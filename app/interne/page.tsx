"use client";import {useState} from "react";import {resources} from "@/lib/resources";export default function Page(){const [q,S]=useState(""),[type,T]=useState("");return <main className="dashboard"><p className="eyebrow">Tout retrouver, simplement</p><h1>Le point d’entrée Solaya.</h1><div className="toolbar"><label>Rechercher<input value={q} onChange={e=>S(e.target.value)} placeholder="Une page, un document, un public…"/></label><label>Type<select value={type} onChange={e=>T(e.target.value)}><option value="">Tous</option>{Array.from(new Set(resources.map(r=>r[2]))).map(t=><option key={t}>{t}</option>)}</select></label></div>{resources.filter(r=>(!type||r[2]===type)&&r.join(" ").toLowerCase().includes(q.toLowerCase())).map(([title,audience,t,url,status])=><article className="resource-row" key={title}><div><a href={url}><strong>{title}</strong></a><p>{audience} · {t}</p></div><div><span>{status}</span><p className="micro">Version de préparation · 14 septembre 2026</p></div></article>)}</main>}