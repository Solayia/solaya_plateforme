export function direct(x){for(const v of Object.values(x))if(!Number.isFinite(v)||v<0)throw Error("Renseignez des montants positifs ou nuls.");if(x.discount>x.revenue)throw Error("La remise dépasse les recettes.");const platform=x.revenue-x.platform-x.management-x.other;const net=x.revenue-x.discount-x.payment-x.acquisition-x.management-x.other;return {platform,direct:net,difference:net-platform}}
export function annual(x){for(const v of Object.values(x))if(!Number.isFinite(v)||v<0)throw Error("Renseignez toutes les hypothèses.");if(x.nights>365||x.rate>100)throw Error("Nuits ou frais incohérents.");const current=x.rent*12-x.currentCosts;const result=x.price*x.nights*(1-x.rate/100)-x.costs;const difference=result-current;return {current,result,difference,payback:difference>0?x.investment/difference:null}}